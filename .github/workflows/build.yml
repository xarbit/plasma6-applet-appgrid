name: Build Packages

on:
  push:
    branches:
      - main
      - 'maintenance/*'
      - 'hotfix-*'
      - 'patch-*'
    paths-ignore:
      - "**.md"
      - "LICENSE"
      - "screenshots/**"
      - ".gitignore"
      - "dev.xarbit.appgrid.metainfo.xml"
      - "PKGBUILD"
      - "packaging/**"
  pull_request:
    branches:
      - main
      - 'maintenance/*'
    paths-ignore:
      - "**.md"
      - "LICENSE"
      - "screenshots/**"
      - ".gitignore"
      - "dev.xarbit.appgrid.metainfo.xml"
      - "PKGBUILD"
      - "packaging/**"
  workflow_dispatch:

permissions:
  contents: read

env:
  BUILD_TYPE: Release

jobs:
  version:
    name: Resolve version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      pkg_version: ${{ steps.version.outputs.pkg_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Determine version
        id: version
        run: |
          BASE_VERSION=$(grep -oP 'project\(AppGrid VERSION \K[0-9]+\.[0-9]+\.[0-9]+' CMakeLists.txt)
          # Dev builds use the workflow run number — it only ever increases,
          # so every publish to the -dev PPA / COPR is unique and correctly
          # ordered. Commits-since-tag was wrong here: it resets after each
          # tag, so COPR / Launchpad rejected the re-used version.
          DEV_NUM="${{ github.run_number }}"
          echo "version=${BASE_VERSION}-dev.${DEV_NUM}" >> "$GITHUB_OUTPUT"
          echo "pkg_version=${BASE_VERSION}~dev.${DEV_NUM}" >> "$GITHUB_OUTPUT"

  tests:
    name: Tests & Lint
    runs-on: ubuntu-latest
    container: archlinux:latest
    steps:
      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm --needed \
            base-devel git cmake extra-cmake-modules \
            qt6-base qt6-declarative \
            libplasma kpackage kio kservice ki18n kiconthemes \
            kcoreaddons kwindowsystem krunner layer-shell-qt \
            plasma-activities plasma-activities-stats \
            plasma-workspace appstream appstream-qt namcap \
            python-pip clang cppcheck
          python3 -m pip install --break-system-packages codespell reuse

      - name: Checkout
        uses: actions/checkout@v5

      - name: Mark workspace as safe (container uid mismatch)
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      # REUSE-IgnoreStart
      - name: Check SPDX license headers
        run: |
          set -e
          files=$(git ls-files '*.cpp' '*.h' '*.qml')
          if [ -z "$files" ]; then
            echo "::error::git ls-files returned no files — repo state broken"
            exit 1
          fi
          missing=$(echo "$files" | while read f; do
            grep -L "SPDX-License-Identifier" "$f" || true
          done)
          if [ -n "$missing" ]; then
            echo "::error::Files missing SPDX-License-Identifier:"
            echo "$missing"
            exit 1
          fi
          echo "all tracked source files carry SPDX-License-Identifier ($(echo "$files" | wc -l) files)"
      # REUSE-IgnoreEnd

      - name: Check trailing whitespace
        run: |
          set -e
          files=$(git ls-files '*.cpp' '*.h' '*.qml' '*.xml' '*.json' '*.md' 'CMakeLists.txt')
          if [ -z "$files" ]; then
            echo "::error::git ls-files returned no files — repo state broken"
            exit 1
          fi
          bad=$(echo "$files" | xargs grep -nP '[ \t]+$' 2>/dev/null || true)
          if [ -n "$bad" ]; then
            echo "::error::Files with trailing whitespace:"
            echo "$bad"
            exit 1
          fi
          echo "no trailing whitespace found"

      - name: Spellcheck (codespell)
        run: |
          codespell \
            --skip='build*,*.po,*.pot,po,*.svg' \
            --ignore-words-list='growin' \
            --quiet-level=2 \
            src/ package/contents/ package/variants/ tests/ README.md

      - name: REUSE compliance
        run: reuse lint || echo "::warning::REUSE lint failed — non-blocking"
        continue-on-error: true

      - name: Build (with tests)
        env:
          CC: clang
          CXX: clang++
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=$BUILD_TYPE -DCMAKE_INSTALL_PREFIX=/usr \
            -DBUILD_TESTING=ON -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
            -DAPPGRID_STRICT_WARNINGS=ON
          cmake --build build -j$(nproc)

      - name: clang-tidy
        run: |
          # Strip gcc-only flags that clang-tidy (clang-based) doesn't recognize
          sed -i 's/-mno-direct-extern-access//g' build/compile_commands.json
          run-clang-tidy -p build -quiet -warnings-as-errors='*' 'src/.*\.cpp'

      - name: cppcheck
        run: |
          cppcheck --enable=warning,style,performance,portability --library=qt \
            --inline-suppr --std=c++20 --language=c++ --quiet --error-exitcode=1 \
            --suppress=returnByReference --suppress=useStlAlgorithm \
            --suppress=knownConditionTrueFalse:src/appgridplugin.cpp \
            -I src $(git ls-files 'src/*.cpp')

      - name: clang-format
        run: |
          git ls-files 'src/*.cpp' 'src/*.h' \
            | xargs clang-format --dry-run -Werror

      - name: Run tests
        env:
          QT_QPA_PLATFORM: offscreen
        run: ctest --test-dir build --output-on-failure

      - name: Validate AppStream metadata
        run: appstreamcli validate --no-net dev.xarbit.appgrid.metainfo.xml

      - name: Validate plasmoid packages
        run: |
          set -e
          mkdir -p /tmp/pkg-validate
          # Both variants are assembled under build/ by the configure step above.
          for pkg in build/package-center build/package-panel; do
            out=$(kpackagetool6 --type Plasma/Applet --install "$pkg" --packageroot /tmp/pkg-validate 2>&1)
            echo "$out"
            if echo "$out" | grep -qE "^Error:"; then
              echo "::error::kpackagetool6 reported error for $pkg"
              exit 1
            fi
          done

      - name: Validate translation files
        run: |
          set -e
          found=0
          while IFS= read -r f; do
            found=1
            msgfmt --check --output-file=/dev/null "$f"
          done < <(find po -name "*.po" -type f)
          if [ "$found" = "0" ]; then
            echo "no .po files to check"
          fi

      - name: Lint QML
        # Lint against the assembled packages (build/ from the configure step)
        # so each variant's main.qml resolves its sibling controllers/views.
        run: |
          /usr/lib/qt6/bin/qmllint -I /usr/lib/qt6/qml \
            build/package-center/contents/ui/main.qml \
            build/package-panel/contents/ui/main.qml \
            build/package-center/contents/ui/iconanims/*.qml \
            build/package-center/contents/ui/prefix/*.qml

      - name: Lint PKGBUILD
        run: namcap PKGBUILD

  universal:
    name: Universal package (${{ matrix.arch }})
    needs: [version, tests]
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        arch: [x86_64, aarch64]
        include:
          - arch: x86_64
            runner: ubuntu-latest
            platform: linux/amd64
          - arch: aarch64
            runner: ubuntu-24.04-arm
            platform: linux/arm64
    env:
      APP_VERSION: ${{ needs.version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Build and package
        # Fedora 42 = oldest mainstream stable shipping Plasma libs at
        # SONAME 7 (KDE bumped from 6 around Plasma 6.4). Built here so the
        # tarball loads on KDE Linux / Kinoite / Bazzite / Aurora / Kalpa /
        # SteamOS / Arch / Tumbleweed. Excludes Plasma <= 6.3 users.
        run: |
          docker run --rm --platform ${{ matrix.platform }} \
            -v ${{ github.workspace }}:/workspace \
            -e APP_VERSION="${APP_VERSION}" \
            -e BUILD_TYPE="${BUILD_TYPE}" \
            -e ARCH="${{ matrix.arch }}" \
            fedora:42 bash -c '
              set -e
              dnf install -y \
                cmake extra-cmake-modules gcc-c++ make \
                qt6-qtbase-devel qt6-qtdeclarative-devel \
                libplasma-devel kf6-kpackage-devel kf6-kio-devel \
                kf6-kservice-devel kf6-ki18n-devel kf6-kcoreaddons-devel \
                kf6-kiconthemes-devel \
                kf6-kwindowsystem-devel kf6-krunner-devel layer-shell-qt-devel \
                plasma-activities-devel plasma-activities-stats-devel \
                plasma-workspace-devel \
                appstream-qt-devel \
                gettext
              cd /workspace
              # Pin plugin install path to lib/qt6/plugins (Fedora ECM
              # defaults to lib64/qt6/plugins, but the universal payload
              # script wires QT_PLUGIN_PATH to ~/.local/lib/qt6/plugins
              # only). NO APOSTROPHES in any comment inside this bash -c
              # block — single quote here closes the outer string.
              cmake -B build -DCMAKE_BUILD_TYPE=$BUILD_TYPE \
                  -DAPPGRID_UNIVERSAL_BUILD=ON \
                  -DKDE_INSTALL_QTPLUGINDIR=lib/qt6/plugins \
                  -DAPPGRID_VERSION_OVERRIDE="$APP_VERSION"
              cmake --build build -j$(nproc)
              mkdir -p out
              bash packaging/universal/build-package.sh \
                "$APP_VERSION" "$ARCH" build out
            '

      - name: Upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: universal-package-${{ matrix.arch }}
          path: out/*

  # Dev channel: every push to main publishes to the -dev PPA / COPR.
  # Pull requests and hotfix-* / patch-* branches build for verification
  # only (the jobs above) and do not publish.
  publish-dev:
    name: Publish dev build
    needs: [version, tests]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: ./.github/workflows/publish.yml
    with:
      channel: dev
      version: ${{ needs.version.outputs.version }}
      pkg_version: ${{ needs.version.outputs.pkg_version }}
    secrets: inherit
